# CRM 数据看板下一阶段方案

日期：2026-07-26  
范围：`/crm/dashboard` 的统计正确性、权限边界与后续工作台演进。

## 目标

先让同一筛选条件、同一角色看到可信且一致的数据；在此基础上，再把看板升级为客服与运营可直接处理待办的工作台。

## 第一阶段：修正统计与权限（发布前必做）

### 1. 统一日期范围语义

- API 接收 `startDate`、`endDate`（均为 `YYYY-MM-DD`），使用业务时区 `Asia/Shanghai`。
- 查询采用半开区间：`created_at >= startOfDay(startDate)` 且 `created_at < startOfDay(endDate + 1 day)`；完成量对应 `finished_at`。
- `startDate > endDate`、非法日期、仅传一个日期和未来日期均返回 400，不再静默忽略。
- 明确字段含义：筛选期间使用 `periodNew`/`periodCompleted`；不要继续把同一个值填入 `weekNew`、`dayNew` 等旧字段。
- 当选择日期范围时，将“医院总数/客户总数”等标题改为“本期新增医院/本期新增客户”，或另行返回存量总数，避免名称与数据不一致。

### 2. 按医院筛选时收敛全部业务实体

给定 `hospitalId` 后：

| 数据 | 过滤规则 |
| --- | --- |
| 医院 | `hospital.id = hospitalId` |
| 派单 | `dispatch.hospital_id = hospitalId` |
| 客户 | 存在属于该医院的有效派单；按客户 ID 去重 |
| 客户状态 | 使用上述客户集合 |
| 趋势、摘要、洞察 | 使用同一客户/派单/医院集合 |

`hospitalId` 的 schema 设为正整数；权限不允许时返回 403（或按产品约定返回空集，但须统一且无信息泄露）。

### 3. 完整角色数据范围

| 角色 | 医院 | 派单 | 客户 |
| --- | --- | --- | --- |
| `super_admin` | 全部，或指定医院 | 全部，或指定医院 | 全部，或指定医院关联客户 |
| `hospital_account` | 仅关联医院 | 仅关联医院 | 仅关联医院涉及的客户 |
| `customer_service` | 不提供医院搜索/筛选 | 仅本人客户的派单；可叠加被授权医院 | 仅 `owner_user_id = 当前用户` |

医院搜索下拉、看板指标和进入列表页的跳转必须复用同一作用域规则。避免通过搜索接口或 URL 参数绕过看板范围。

## 第二阶段：自动化验收（与第一阶段一起完成）

新增 CRM 看板集成测试，至少覆盖：

1. 超级管理员：全量、指定医院、日期边界（开始日、结束日当天、跨月）。
2. 客服：只能看到本人客户及其派单；医院参数不会扩大数据范围。
3. 医院账号：只能看到关联医院、其派单和关联客户；搜索结果不含其他医院。
4. 非法参数：`hospitalId=0`、越权医院、单边日期、无效日期、开始晚于结束。
5. 筛选一致性：同一条件下指标、状态分布、趋势和摘要使用同一数据集。

验收标准：三角色均不存在范围外记录，结束日全天记录都被统计，任一无效/越权筛选不会退化为全量数据。

## 第三阶段：待办工作台（P1）

在数据正确后，将首屏核心卡替换为可执行工作队列：

- 待派单客户
- 待医院回复派单
- 超过 SLA 未跟进派单
- 即将到院确认

每项返回数量、最早等待时间、阈值和稳定 ID 筛选条件；点击后进入对应列表。客服优先展示个人待办，医院账号展示本院待回复，超级管理员展示全局和分医院汇总。

## 第四阶段：经营分析基础设施（按业务确认后实施）

以下不是当前发布阻塞项，需在确认指标口径和运营动作后实施：

- 状态事件/快照表：用于真实批次漏斗、到院/成交转化、SLA 和首次回复时长。
- 医院效率榜：以最小样本量保护小样本误导，按派单量、响应时效、到院率、成交率计算。
- 后端可执行洞察：返回问题、证据、建议动作和目标列表条件，替换前端固定阈值提示。

在这些基础设施完成前，页面继续使用“客户状态结构”和“本期摘要”的名称，不把它们表达为真实漏斗或操作事件流。

## 实施顺序

1. 修正日期半开区间、参数校验和按医院客户收敛。
2. 统一三类角色的作用域，并修复医院搜索与列表跳转。
3. 补齐集成测试并通过验收标准。
4. 上线可信的统计看板。
5. 迭代待办工作台；确认业务口径后再建设事件/快照与后端洞察。
