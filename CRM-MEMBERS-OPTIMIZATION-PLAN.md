# CRM 会员顾客页优化方案

日期：2026-07-26  
范围：`/crm/members` 的会员列表、顶部概览与客服待办效率。  
关联页面：`/crm/dashboard`（经营分析），但本方案不将会员页扩展为第二个数据看板。

## 目标

让会员页从“可查询、可编辑的资料列表”成为客服每日可直接处理的工作台：快速发现需处理会员、按条件进入队列、完成操作后即时看到待办变化。

## 当前情况与问题

| 项目 | 当前实现 | 问题 |
| --- | --- | --- |
| 顶部统计 | 四张 `Card + StatItem` | 仅请求并赋值了 `stats.total`，其余三项会长期显示 `-`。 |
| 统计来源 | 用 `getMembers({ page: 1, pageSize: 0 })` 推导 | 列表接口不应承担业务概览；口径不完整，且操作后不会刷新。 |
| 卡片作用 | 只展示数字 | 无法进入对应待办列表，不能缩短处理路径。 |
| 筛选 | 已有阶段、品类、意向等级等基础筛选 | 后端已有的负责人、来源、标签、跟进时间、逾期等能力没有完整暴露。 |
| 响应式 | 四个 `Col span={6}` | 小屏会保持四列，内容容易拥挤；与现有 dashboard 的栅格规范不一致。 |
| 工具栏 | 包含导出按钮 | 尚未绑定导出行为，不应给出不可用的操作承诺。 |

代码定位：`apps/yishan-admin/src/modules/crm/pages/members/index.tsx`。

## 设计原则

1. 会员页服务“处理待办”，Dashboard 服务“经营分析”。会员页只保留一行高价值指标。
2. 每张指标卡必须有明确业务口径、加载/失败状态和可执行去向。
3. 卡片和列表共享筛选条件、数据权限及时间边界，避免“卡片数量”和“列表数量”不一致。
4. 操作成功后刷新表格和概览；不以整页刷新作为状态同步手段。
5. 复用项目 dashboard 的 `xs: 24, sm: 12, lg: 6` 栅格与卡片视觉规范。

## 推荐首屏

### 一行可行动的指标卡

| 卡片 | 主指标 | 辅助信息 | 点击后列表条件 |
| --- | --- | --- | --- |
| 待办跟进 | 待跟进数 | `其中 N 个已逾期`；逾期时红色强调 | `nextFollowUpEnd=当前时刻`，默认按下次跟进升序 |
| 今日新增 | 当日新会员数 | 较昨日增减（有同比口径后再显示） | 今日 `createdStart/createdEnd` |
| 本月派单 | 本月已派单会员数 | 派单率或较上月变化（二选一） | `memberStage=dispatched` 加本月范围 |
| 本月成交转化 | 成交率 | 成交人数 / 进入转化口径的人数 | `memberStage=converted` 加本月范围 |

卡片使用 `StatisticCard`（或封装为与其相同结构的项目通用 `MetricCard`）：标题、主数值、趋势/辅助说明、底部操作文案。不要只用图标加大数字；待办和逾期应有可辨识的状态层级。

### 快捷筛选

在表格搜索区前或 `ProTable` 工具栏中提供可切换的预设：

- 全部会员
- 我的会员（固定当前用户为负责人）
- 今日待跟进
- 已逾期（高风险状态，显示数量 Badge）
- 本周新增

选择预设时应同步更新表单值、URL 查询参数和数据请求；手动修改高级筛选后取消预设的 active 状态。

### 高级筛选补齐

在已有会员阶段、业务类别、意向等级基础上增加：负责人、来源渠道、会员标签、下次跟进日期范围、创建日期范围、是否逾期。搜索输入应明确支持“姓名 / 手机号 / 会员编号”。

## API 与数据口径

新增只读接口：`GET /api/crm/v1/members/overview`。

接口须和 `GET /members` 复用同一个数据权限作用域：超级管理员看全量，客服仅看本人拥有的数据，医院账号仅看其授权范围。接口返回示例：

```ts
type MemberOverview = {
  total: number;
  todayNew: number;
  pendingFollowUp: number;
  overdueFollowUp: number;
  monthDispatched: number;
  monthConverted: number;
  monthConversionRate: number | null;
  generatedAt: string;
};
```

口径约定：

- 今天、本月按 `Asia/Shanghai` 计算；开始时间包含、结束时间排除。
- `pendingFollowUp` 为有效会员且设置了下次跟进时间、未完成跟进的会员；若当前业务暂无“完成”标记，应先明确以“下次跟进时间不为空”还是以阶段集合定义。
- `overdueFollowUp` 是待跟进中 `nextFollowUpAt < now` 的子集。
- `monthDispatched` 和 `monthConverted` 应基于阶段变化事件/派单记录计算；若当前只有最终 `memberStage`，先命名为“当前已派单 / 当前已成交”，避免误称“本月”。
- `monthConversionRate` 的分母必须由产品确认（例如当月新增、当月进入有效跟进、或当月派单）；分母为零时返回 `null`，前端显示 `-`。

## 前端实现建议

1. 抽出 `useMemberOverview()`：负责加载、错误、刷新和请求序列保护；不要在列表数据请求中推导指标。
2. 将四张卡定义为配置数组，统一处理 loading、空值、点击、键盘可访问性与 Tooltip。
3. 使用响应式栅格：`xs: 24, sm: 12, lg: 6`，`gutter: [16, 16]`。
4. 操作成功（新增、编辑、跟进、派单、批量作废/恢复/分配）后同时执行 `actionRef.current?.reload()` 和 `reloadOverview()`。
5. 卡片点击通过 `formRef` 或受控查询状态设置表格条件，并重置至第一页；条件编码到 URL，便于分享和返回。
6. 将静态的“共 N 条会员记录”替换为真实 `total`，或移除 tooltip。
7. 导出功能完成前隐藏按钮；实现时导出必须继承当前筛选和数据权限，并提供异步任务状态、下载入口和失败提示。

## 与 Dashboard 的边界

| 留在会员页 | 留在 CRM Dashboard |
| --- | --- |
| 待办跟进、逾期、今日新增、当前处理队列 | 趋势、漏斗、渠道/品类结构、医院排行、经营洞察 |
| 点击卡片后进入已筛选的会员表格 | 时间范围和医院维度的横向分析 |
| 单会员和批量处理操作 | 指标解释与跨实体分析 |

## 实施分期

### P0：保证数据可信

1. 建立 `members/overview` 接口和 schema，复用数据权限。
2. 明确待跟进、逾期、本月派单、成交转化的业务口径。
3. 为时区边界、角色范围、空分母、逾期边界和操作后的刷新补充测试。

验收：四张卡不再出现因未赋值导致的 `-`；任意角色打开卡片与进入列表看到同一数据范围。

### P1：转化为工作台

1. 以 `StatisticCard`/通用 `MetricCard` 重建顶部卡片。
2. 完成快捷筛选、URL 同步和卡片跳转。
3. 补齐负责人、来源、标签和跟进时间等高级筛选。
4. 移除未实现的导出入口或实现受权限约束的异步导出。

验收：客服能从“已逾期”卡片进入目标列表，并完成跟进后看到逾期数下降；移动端卡片为单列/双列且不拥挤。

### P2：体验完善

1. 增加指标趋势（需先确认比较周期和数据来源）。
2. 增加保存的个人筛选视图，例如“我的高意向未跟进”。
3. 依据数据规模实现服务端导出、任务中心和审计记录。

## 参考

- [ProCard](https://procomponents.ant.design/components/card/)
- [StatisticCard](https://procomponents.ant.design/components/statistic-card/)
- [Ant Design Pro Dashboard](https://github.com/ant-design/ant-design-pro/tree/master/src/pages/dashboard)
