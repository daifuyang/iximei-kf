# Phase A Bugfix 交付总结（2026-07-28）

> **范围**：用户原话 6 项中前 3 项（①手机号精确 / ②400 修复 / ③医院登录诊断）— 即 CRM bugfix Phase A。
> **状态**：✅ 6 commits 在 main 头 + feature branch `fix/crm-phase-a-bugfixes`，final review Approved。
> **分支**：`fix/crm-phase-a-bugfixes`（与 main 同 commit hash `c1e2f49`，fast-forward merge 状态）
> **关联文档**：
> - Spec: `docs/superpowers/specs/2026-07-28-crm-bugfixes-design.md`
> - Plan: `docs/superpowers/plans/2026-07-28-crm-bugfixes.md`
> - 根因报告（会员创建 400）: `docs/superpowers/specs/2026-07-28-member-creation-400-rootcause.md`
> - 根因报告（医院登录）: `docs/superpowers/specs/2026-07-28-hospital-login-rootcause.md`
> - 完整 ledger: `.superpowers/sdd/2026-07-28-crm-bugfixes/progress.md`（gitignored）
> - Final review: `.superpowers/sdd/2026-07-28-crm-bugfixes/final-review.md`（gitignored）

---

## 1. 用户需求覆盖

| 需求 | 描述 | commit(s) | 状态 |
| --- | --- | --- | --- |
| ① | 顾客转会员：手机号精确查询 | `bacdd71` + `fca54b3` + `7bfe229` | ✅ |
| ② | 会员顾客创建 400 错误 | `92a1331` + `e4e1939` | ✅ |
| ③ | 医院账号无法登录医院后台 | `c1e2f49` | ✅ |
| ④ | 总后台增加「医院查看顾客记录」 | — | 🚫 Phase B |
| ⑤ | 医院数据看板 | — | 🚫 Phase B |
| ⑥ | 医院未查看订单提醒 | — | 🚫 Phase B |

---

## 2. 交付清单（6 commits）

| Commit | Message | 改动 |
| --- | --- | --- |
| `bacdd71` | fix(crm): support exact mobile / prefix name match for customer selection | 后端 schema 加 mobile/name optional + repository 三级回退 + 4 vitest |
| `fca54b3` | feat(crm-admin): exact mobile search + not-found empty state | 前端 search 切换 + 「未找到对应顾客」空态 + debounce 反查用 mobile |
| `7bfe229` | chore(openapi): regenerate crm client after mobile/name query fields | typings.d.ts + openapi.json 同步 |
| `92a1331` | docs(crm): 400 root cause analysis for member creation | 329 行根因报告（4 风险点 + 调用栈 + actionable findings） |
| `e4e1939` | fix(crm-admin): normalize payload dates + localized api errors | utils.ts (normalizeMemberPayload + extractApiError) + 7 处 catch 替换 + 12 jest |
| `c1e2f49` | feat(scripts): diagnose + fix hospital account anomalies (dry-run default) | diagnose SQL + fix 脚本（dry-run 默认） + 根因报告 |

---

## 3. 关键工程决策（R1-R6）

- **R1** T2→T5 顺序派发避免 index.tsx merge 冲突（验证无冲突）
- **R2** T3 OpenAPI 在 T1 schema 后、T5 工具函数前同步（验证时序正确）
- **R3** A.3 dry-run 默认 + `--apply` 缺 `--default-password` 立即 `process.exit(2)`（双重安全门）
- **R4** name 字段用 `${q.name}%` 前缀 LIKE（非子串模糊）
- **R5** 工具函数 `utils.ts` export 供 jest import（12/12 测试过）
- **R6** 不动 toolchain（Node 22.22.1 / pnpm 8.15.9 保持）

---

## 4. Quality Gate（T7 验证）

| Gate | 命令 | 结果 |
| --- | --- | --- |
| Lint | `pnpm lint` | 0 errors, 14 historical warnings（Phase A 范围外） |
| Admin test | `pnpm --filter yishan-admin test` | 33 passed |
| API test | `pnpm --filter yishan-api test` | 334 passed, 20 skipped（integration） |
| Admin build | `pnpm --filter yishan-admin build` | exit 0（umi 6 chunks） |

---

## 5. Follow-up 7 条（不阻塞，建议另立 ticket）

1. **14 条历史 lint warnings**（Phase A 改动范围外）—— 历史累积
2. **openapi.json 治理 ticket**（含 M-T3.1/2/3 三条 minor）：
   - 加 `apps/yishan-api` 的 `openapi:export` 脚本（避免手工 patch）
   - CI 加 `openapi.json` ↔ 后端 routes 一致性校验
   - openapi.json 多行化（prettier）
   - 清理 `/api/crm/v1/customers/selectable` 历史 duplicate 路径
   - 把 `CrmCustomerSelectableQuery` 注册到 `components.schemas` 并 `$ref`
3. **crm admin API adapter 类型收紧** —— `crm/api/index.ts` 多处 `request<any>` wrapper 改为消费 generated types
4. **`createMemberTag` line 1255 catch 未替换**（T5 范围外第 8 处）
5. **`fix-hospital-accounts.ts` 可选增强** —— 加 `--creator-id` CLI 参数；username 加 sanitize
6. **`api` build:ts 未在 CI gate 触发** —— M-T1.5 兜底缺失
7. **Phase B 准备** —— ④⑤⑥ 业务闭环（医院查看顾客记录 / 数据看板 / 未查看订单提醒）需另立 design doc + plan

---

## 6. 验收清单

- [x] Phase A 三项用户原需求（①+②+③）全覆盖
- [x] ④⑤⑥ 显式 out-of-scope（Phase B）
- [x] 6 个 ledger rulings (R1-R6) 严格遵守
- [x] 6 个 task-level review 全部 ✅ Approved，零 critical/important 残留
- [x] 17 条 minor 串接后不构成 blocking 风险
- [x] Working tree clean，Task 7 quality gate 三道门全绿
- [x] 所有 commit Conventional Commits 格式
- [x] git history 干净，无空 commit 噪声
- [x] Feature branch `fix/crm-phase-a-bugfixes` 已建（与 main fast-forward 关系）

---

## 7. 环境踩坑备忘（未来参考）

- **npm 镜像慢**：`pnpm config set registry https://registry.npmjs.org/` 备援（npmmirror 大包 30s+，官方 15s/64MB）
- **系统代理 127.0.0.1:7890 干扰**：所有 pnpm/curl 命名前 `unset http_proxy https_proxy all_proxy`
- **`pnpm --filter` 触发 confirmModulesPurge**：加 `--config.confirmModulesPurge=false`

---

**结论**：Phase A 全部完成，可发布。Phase B（医院查看顾客 / 数据看板 / 未查看订单提醒）需另立 design doc + plan。
