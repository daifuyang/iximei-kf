# Phase B 交付总结（2026-07-28）

> **范围**：用户原话 6 项中第 4-6 项 = 完整业务闭环（医院查看顾客状态 + 医院数据看板 + 未查看订单提醒）
> **状态**：✅ 12 commits 在 main 头，final review Approved，ready to merge
> **关联文档**：
> - Spec: `docs/superpowers/specs/2026-07-28-crm-view-tracking-design.md`
> - Plan: `docs/superpowers/plans/2026-07-28-crm-view-tracking.md`
> - Final review: `.superpowers/sdd/2026-07-28-crm-view-tracking/final-review.md`（gitignored）

---

## 1. 用户需求覆盖

| 需求 | 描述 | commit(s) | 状态 |
| --- | --- | --- | --- |
| ④ | 总后台增加「医院查看顾客记录」 | `331ce2e`+`a01f4bb`+`ab68939`+`e014d5a`+`8d8b349`+`3bbb138` | ✅ |
| ⑤ | 医院后台数据看板 | `ac98fa6`+`a0d02a6` | ✅ |
| ⑥ | 医院未查看订单提醒 | `ac98fa6`+`1e16a14`+`d806dfb` | ✅ |

**业务闭环**：
```
总后台派单 → 派单 status='未查看' → 医院后台看到新派单 → 菜单 Badge 显示数量
→ 医院点详情 → 自动写 crm_dispatch_view_log → 数量 -1
→ 总后台派单详情看到「医院查看状态」表格
```

---

## 2. 交付清单（12 commits）

| Commit | Message | 改动 |
| --- | --- | --- |
| `331ce2e` | feat(crm): crm_dispatch_view_log schema + migration | 1 表 + 索引 + 4 vitest |
| `a01f4bb` | feat(crm): dispatch view log repository + tests | recordView + listViewLogs |
| `ab68939` | feat(crm): auto-record dispatch view on hospital account access | service getById 嵌入 + 2 权限 |
| `e014d5a` | feat(crm): dispatch hospital view logs API | listCrmDispatchHospitalViewLogs 路由（fix: hospitalName mapper） |
| `ac98fa6` | feat(crm): hospital backend dashboard + unviewed badge APIs | 5 新文件 + 路由挂载 |
| `8d8b349` | feat(crm-admin): dispatch detail shows hospital view status | 前端 ProDescriptions 首次尝试 |
| `3bbb138` | fix(crm-admin): hospital view status uses ProTable + viewerUsername truthiness | T6 fix: ProTable 替换 |
| `a0d02a6` | feat(crm-admin): hospital backend dashboard page | 7 张 Card 看板 |
| `601b929` | chore(openapi): regenerate crm client after view-tracking | 3 new operationId |
| `6a1880d` | feat(crm): seed hospital-dashboard menu + view-log permissions | system-menu.json + 角色种子 |
| `1e16a14` | feat(crm-admin): hospital menu badge shows unviewed dispatch count | 菜单 Badge 轮询 |
| `d806dfb` | refactor(crm-admin): replace role id magic numbers with ROLE_IDS enum | T10 fix: enum 引用 |

---

## 3. 关键工程决策（R1-R5 + 3 fix）

- **R1**: T2→T3→T4 顺序派发；getById 加 req 参数一次 3 文件同步
- **R2**: T5 dashboard API 先于 T6/T7 前端
- **R3**: T6 引用 T8 API 前用 `request<any>` 兜底
- **R4**: T9 seed menu 字段与 component 严格匹配
- **R5**: T10 探测 menu 后选方案 A（ProLayout `menuItemRender` + 桥接组件）
- **T4 fix**: hospitalName wire mismatch → 显式 row mapper 参照 sibling `listDispatchMobileViews`
- **T6 fix**: ProDescriptions 误用 → ProTable + viewerUsername truthy render
- **T10 fix**: magic number `3` → `ROLE_IDS.HOSPITAL_ACCOUNT` enum 引用（顺手修 app.tsx:306 `1` → `ROLE_IDS.SUPER_ADMIN`）

---

## 4. Quality Gate

| Gate | 结果 |
| --- | --- |
| Lint | 0 errors (14 历史 warnings，Phase A 范围外) |
| Admin test | 33/33 passed |
| API test | 341 passed + 20 skipped (integration) |
| Admin build | Built in 12.3s, exit 0 |

---

## 5. 6 条 minor（不阻塞，建议后续 ticket）

1. **T6 留下 dead state**（dataSource 在 ProTable 切换后未清理）
2. **T9 seed 未实跑**（环境无 MySQL；用 tsc + 静态阅读保证）
3. **spec §4.2 主响应体 `viewedByCurrentUser` 增强未实现**（spec §9 验收通过抽屉 ProTable 闭环替代；如需主响应体也加需后续 ticket）
4. **T4 F3 response schema file-wide convention defer**（与 sibling 路由保持一致；全文件 sweep 是另立 ticket）
5. **T8 openapi.json cosmetic 多行化**（18744 行 diff；语义等价）
6. **region 白名单 Phase A baseline 重复**

---

## 6. 验收清单

- [x] Phase B 三项用户原需求（④+⑤+⑥）全覆盖
- [x] 11 个 task-level review 全部 ✅ Approved
- [x] 3 个 fix rounds 全部 ADDRESSED
- [x] 6 minor 串接后不构成 blocking 风险
- [x] Working tree clean，Task 11 quality gate 三道门全绿
- [x] 所有 commit Conventional Commits 格式
- [x] git history 干净，无空 commit 噪声

---

## 7. 环境踩坑备忘（与 Phase A 共享）

- 跑命令前 `unset http_proxy https_proxy all_proxy`
- pnpm filter 加 `--config.confirmModulesPurge=false`
- `~/.gitconfig` 已配 `[url "git@github.com:"].insteadOf = https://github.com/`（所有 https github 自动 SSH）
- yishan-tiptap 先 build，admin tsc 才 0 errors

---

**结论**：Phase A + Phase B 全部完成，用户原话 6 项 100% 落地。可发布。
